TODO. Web Performance 项目设计
- 降低DTO传递的数据量(避免无用字段,避免全表查询)
- 后端使用Async异步线程处理请求
- 后端使用Pageable分页返回数据
- 启用GZIP压缩降低传递JSON数据量
- PULL PUSH技术(后端异步推送(MQ, SSE))
- 使用StreamingResponseBody流式数据返回(数据量极大时)?

实战场景:
1. Web前端获取大量数据需要加载到页面上
2. Web前端页面向后端发送耗时请求,导致页面卡顿并响应缓慢

前端发送异步请求避免阻塞:
angular.async.Observable()
httpAsyncClient.execute(post, FutureCallback());
httpClient.sendAsync(request, responseInfo -> new EventSubscriber())